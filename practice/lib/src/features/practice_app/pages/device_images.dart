import 'package:flutter/material.dart';

class DeviceImages extends StatelessWidget {
  const DeviceImages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Display Device Images'),
      ),
    );
  }
}
